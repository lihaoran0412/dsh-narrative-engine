# 角色设计

本文件只负责人物层：生成原则、起名、人物决策轴、主 NPC 角色卡、身份档、处境档、亲密画像、决策卡、角色层级、配角和玩家化身。执行时机、结构骰与开局校验以 `references/开局流程.md` 为准；身份、处境与壳的抽样表见 `references/素材库.md`；安全不变量以 `SKILL.md` 为准。

主要角色和亲密互动参与者必须明确为 18 岁以上；校园、社团、学园或师生意象须声明为成年人教育、职业培训或成年社群。

## 人物生成原则

先生成普通人格、目标、底线和生活处境。

生成时先抽取宽泛倾向，再分别生成各字段。倾向只调整概率，不锁定结果；主 NPC 可提高少见组合概率，普通配角以自然普通为主。同局人物不得全部使用同一倾向。

```yaml
profile_weights:
  ordinary_natural: 35
  reserved_sensitive: 15
  inexperienced: 12
  experienced_restrained: 10
  open_active: 8
  playful: 8
  conflicted: 7
  low_desire: 5
```

所有画像仅用于明确为成年人的虚构角色。知识少、经验少、单纯或不懂世事不得被解释为年龄不明或未成年。

<!-- 维护契约（改动前必读；本文件下列小节被 scripts/roll_opening.py 实时解析）：
1. 「表层风味」「口癖与语感」的分组行必须保持 `- 组名：项、项`；表层风味和口癖单项超过 8 字会被脚本筛掉。
2. 「外观与气质轴」的顶层子轴必须是无冒号的 `- 轴名`，其下使用缩进的 `  - 组名：项、项`；缩进改变会使子轴失联。
3. 「人物决策轴」必须保持 `- 轴名：项、项、项`，且至少有两项。
4. 「人物生成原则」代码块中的权重必须保持两空格缩进的 `key: 数字` 格式，否则权重会退回默认值。
5. 「配角」小节必须保留 `配角功能独立生成：…。` 句式，功能项以顿号或“或”分隔。
6. 以上小节标题是脚本的前缀锚点；改名会使对应解析失联。改动后运行 `python scripts/roll_opening.py --seed 1 --no-history` 核对各轴非空。
-->

## 可选修饰层

### 修饰层通则

以下修饰层均可选，共享同一组规则，各子表不再逐条重复：

1. 只影响语言滤镜、画面质感、标志动作与相处质感，不改变人格、目标、底线、亲密画像或许可判定。
2. 同局不同角色不重复同一项；各修饰层与亲密画像分别独立抽取，互不推导。
3. 抽取遵循 `references/开局流程.md`「随机性与多样性协议」。
4. 美学基调为「青年漫写实」或「写实文学」时，表层风味与口癖跳过（写 `—`）；外观轴仍可用，但只取写实项。

主 NPC 或重要配角可各抽用下列三层；外观轴主 NPC 至多两项显著特征、配角至多一项，其余每层至多一项。抽用结果写入 9f 语言滤镜或外貌细节，不新增存档字段。

### 表层风味

分类（以下各组条目的并集即为正式表层风味池；抽取时不先抽分类，分类仅用于检索、去重与生成解释）：
- 傲娇反差：傲娇、冷娇、逞强嘴硬、扮猪吃虎、闷骚、装酷内热、假高冷
- 冷淡疏离：三无、清冷、孤高、出尘淡漠、清冷寡言、疏离淡漠、清寂、冰山、淡泊寡欲、清高自持
- 温柔亲和：温柔年上、治愈系、温婉、体贴包容、忠犬、老好人、温润如玉、和煦亲切、柔声细语、包容宽厚、耐心守护
- 端庄教养：姐御、端庄淑女、矜持知礼、大家闺秀、老成持重、温雅端方、世家教养、持重稳妥、恭谨守礼、雍容端庄
- 傲慢高冷：傲慢公子、傲慢千金、大小姐、自恋狂、高傲、目中无人、恃才傲物、睥睨众生、颐指气使、目下无尘
- 活力外放：元气、大大咧咧、吃货、戏精、中二、俺样
- 危险腹黑：腹黑、病娇、小恶魔、笑面藏刀、天然黑、毒舌、吐槽役
- 认真内敛：书呆子、认真过头、正经人崩坏、社恐结巴、武痴
- 其他单体：天然呆、病弱、慵懒摸鱼、护短、爱哭鬼、迟钝木讷、系统自拟

### 外观与气质轴

现实档中的非自然发色瞳色写为染发、美瞳，或顺延为自然色。各子轴分组条目的并集即为该子轴正式池；抽取时不先抽分组，分组仅用于检索、去重与生成解释。

- 发色
  - 自然发色：黑、深棕、亚麻、栗色、奶茶色、蜜金
  - 冷调染色：银白、铂金、雾灰、雪青、蓝紫
  - 暖调/艳色染色：酒红、粉、脏橘、墨绿
  - 特殊工艺：渐变、内侧挑染、鸳鸯双色
- 发型
  - 短发：短碎、寸头、齐耳短发、飞机头
  - 中长/直发：长直、及肩内扣、偏分长发、齐刘海
  - 束发扎发：高马尾、双马尾、丸子头、挽髻盘发
  - 编织/造型：编发、波浪卷、狼尾、脏辫、麻花辫、公主切
- 瞳与面部
  - 瞳色：琥珀瞳、深黑瞳、灰蓝瞳、翠绿瞳、金棕瞳、异色瞳
  - 眼型：下垂眼、锐利吊眼、无高光眼、猫眼瞳、桃花眼、单眼皮
  - 面部特征：泪痣、虎牙、雀斑、卧蚕、酒窝、薄唇
- 体型气质
  - 体格线条：高挑、娇小、修长、挺拔、敦实、健壮、精悍
  - 柔软丰盈：柔韧、丰腴、窈窕、柔美
  - 清瘦纤细：清瘦、单薄、书卷气、恬静
  - 端雅气度：端正、端丽、娴雅、雍容、温婉
  - 冷感锋锐：锋利、气场压迫、清冷、冷艳
  - 松弛慵懒：慵懒、温吞
- 标志配饰
  - 头面佩戴：眼镜、发饰、发带、口罩、鸭舌帽
  - 首饰饰品：耳骨钉、吊坠项链、戒指、护身符
  - 服饰配件：露指手套、围巾、领带、徽章
  - 随身物件：旧手表、耳机
  - 身体印记：纹身、旧疤、创可贴

### 口癖与语感

分类（以下各组条目的并集即为正式口癖池；抽取时不先抽分类，分类仅用于检索、去重与生成解释）：
- 句式节奏：句尾口癖、极简应答、语速过快、拟声词多
- 语域腔调：敬语过剩、书面语腔、突然方言
- 表达策略：委婉绕圈、比喻控、总在道歉
- 称呼习惯：称呼独特、无敬语直呼

## 人物决策轴

为主 NPC 逐轴抽取；配角只取核心价值与压力策略。它们是生成目标、底线和决策卡的种子，不新增存档字段，也不推导亲密画像或当前许可。

- 核心价值：自由、责任、归属、真相、秩序、成就、公平、安稳、照顾他人、自我证明
- 压力策略：正面解决、谈判交换、隐瞒拖延、寻求帮助、独自承担、转移风险、收集证据、先退后进、用行动代替解释、维持表面稳定
- 关系姿态：谨慎试探、直接合作、保持距离、过度负责、习惯掌控、害怕亏欠、先信后验、先验后信、用玩笑降温、用规则保护双方

同局角色尽量不重复同一组合。三轴是生成种子，其落地映射（核心价值→稳定核心/目标，压力策略→压力反应/行动偏好，关系姿态→性格与行为证据）统一见「角色决策卡」，此处不重复；不得把标签原样堆进人物介绍。反差轴（表见 `references/素材库.md`）仅落入性格层与相处质感证据，不新增字段，不反推亲密画像或许可。

## 名字生成与去重

起名是开局关卡，不是润色。名字由生活背景决定，不由性格、欲望或反差设定决定。禁止跳过、后补或先写正文再起名。

### 分层要求

| 角色 | 流程 | 候选 | 审计深度 |
|---|---|---|---|
| 主 NPC | 标准起名 | ≥3 | 标准 `naming_audit` |
| 未预锁玩家化身 | 标准起名 | ≥3 | 标准 `naming_audit` |
| 预锁玩家姓名 | 合规与去重 | — | `locked_by_player: true` + checks |
| 配角 | 简起名 | ≥2 | 简 `naming_audit` |

时机：9a 先完成主 NPC 起名再写年龄/身份；步骤 13 每名配角先简起名；玩家化身按上表。开局校验时任一起名不合格则 `validation.passed: false`。

### 标准起名（主 NPC / 未预锁玩家）

1. 建立 `name_profile`（culture / region / era / social_context / usage）。
2. 生成至少 3 个符合背景的候选（中文：先姓后名再组合）。
3. 排除同局重名、读音冲突、文化明显不符或象征意味过强的候选。
4. 在剩余候选中选择最自然、最符合角色生活背景且便于区分的姓名；不得按性格、外貌或“最有故事感”选名。
5. 写入 `naming_audit` 与姓名字段后，才写其余人物字段。

### 简起名（配角）

1. 继承本局主文化/地区/年代，可只写精简 `name_profile`。
2. 生成至少 2 个更常见、易区分的候选。
3. 选择与同局不冲突、自然普通的姓名，不做分项评分。
4. 写入简审计后继续配角其余字段。

### 审计结构

标准与简起名共用同一结构，用 `mode` 区分。`checks` 是唯一判定源：任一项为 `fail` 即该角色起名不合格，开局校验 `validation.passed: false`。standard 要求 `name_profile` 完整、候选 ≥3、`chosen` 非空、不按人设/外貌选名；lite 要求候选 ≥2、`chosen` 非空、`supporting_plainer` 通过。这些要求均已由对应 checks 项覆盖，不另列散文清单。

```yaml
naming_audit:
  role_ref: main_npc|player|supporting
  mode: standard|lite            # 主 NPC / 未预锁玩家 = standard；配角 = lite
  name_profile: {culture: null, region: null, era: null, social_context: null, usage: legal_name}
  candidates:                    # standard ≥3；lite ≥2
    - {name: null, reject_reason: null}
  chosen: null
  checks:
    same_cast_duplicate: pass|fail
    phonetic_clash: pass|fail
    culture_match: pass|fail
    natural_fit: pass|fail
    supporting_plainer: pass|fail|n_a   # 仅 lite 检查；standard 写 n_a
  recent_check: checked|unavailable
  locked_by_player: false
```

### 姓名字段

```yaml
legal_name: null
public_name: null
nickname: null
alias: null
```

只填剧情需要的字段。`naming_audit.chosen` 默认写入 `legal_name`。近期记录仅参与去重，不得改写预锁姓名。命名检查直接依据角色所在文化、地区、年代和社会背景，不依赖外部命名权重文件。

## 主 NPC 全维角色卡

### 9a-9g

| 子步 | 内容 |
|---|---|
| 9a | 标准起名后写入姓名、年龄（≥18）、身份（按「身份档」补齐）、公开名声 |
| 9b | 处境类型、具体压力、局势句（按「处境档」补齐） |
| 9c | 人物决策三轴，再加可选反差轴（抽样表见本文件与 `references/素材库.md`；均不锁定亲密画像） |
| 9d | 亲密画像开局核心子集与变化基线 |
| 9e | 对玩家初始关系 −5..+5，并写出原因 |
| 9f | 语言滤镜：句长、礼貌度、回避方式、压力下口吻；抽取了表层风味时须在此体现；进入亲密线的角色按「表里双语态」补齐两档 |
| 9g | 生成清单核对（索引，不重复写内容） |

9a 先完成标准起名再写其余字段。禁止先定人设/反差/画像再回头配名。无合格姓名不得进入 9b。

### 9g 生成清单

作为核对索引，确认结果已存在即可，勿再生成第二套卡。以下项已在 9a-9f 或引用结构中产出，此处只核对不重写：外貌与识别细节（9g-1，可抽外观轴）、核心性格与三轴表现（9c）、目标与利害（9b/决策卡）、底线与拒绝方式（决策卡）、语言滤镜与双语态（9f）、亲密画像与变化基线（9d）。

9g 只需显式补齐下列尚未在别处产出的项：

1. 标志动作
2. 已知 / 未知信息
3. 近期记忆 1-2 条
4. 当前情绪
5. 资源与把柄
6. 地点与行动窗口
7. 亲密边界与撤回信号（关系阶段 ≠ 当前同意）
8. 自主冷却初始值

## 身份档（identity_profile）

先选择身份族，再选择或自拟具体身份（抽样表见 `references/素材库.md`「身份侧」）。身份必须能解释角色拥有的资源、限制与社会风险。生成后只需补齐以下最小关系：

```yaml
identity_profile:
  role: null               # 校验器恢复锚点；与 public_identity 同义时复述
  public_identity: null    # 对外身份
  actual_function: null    # 实际职能；与公开身份一致则复述
  authority_source: null   # 权限/资源凭什么成立
  key_resource: null
  limitation: null
  obligation: null
  exposure_risk: null      # 暴露代价与社会风险（合并原 social_risk）
  hidden_mismatch: null    # 无隐藏身份则写 —
```

公开名声沿用 9a 已生成的字段，身份档只引用不重存。`hidden_mismatch` 可为空，不要求每名角色都有隐藏身份。区分「侍奉与身契」「成人行业与感官服务」「私密撮合与契约中介」时，先看人身关系与服务位置：前者含依附或契约义务，中者是可自主进出的职业，后者只负责撮合与保密安排，不直接提供感官服务。身份侧只描述“这个人凭什么能做什么”，不替代处境压力、角色态度或许可判定。

涉及反差时，先生成公开身份与私下场景的差异，再由独立亲密画像决定欲望与表现；身份不等于性感或许可，`exposure_risk` 只表示暴露代价。

## 处境档（situation）

处境只决定角色正在承受的压力，不决定服从或许可（抽样表见 `references/素材库.md`「处境侧」）。正式结果只需补齐以下字段：

```yaml
situation:
  type: null
  trigger: null
  pressure: null
  immediate_objective: null
  deadline: null
  unresolved_choice: null
  knowledge_gap:
    player_knows: []      # 例：对方的真实身份
    npc_knows: []         # 例：门外还有第三方在等
    both_mistake: []      # 例：双方都以为对方已同意
  exits:
    available: true
    cost: null
    blocked_by: null
  consequence:
    immediate: null
    near_term: null
```

`type` 说明处境形态，`trigger` 说明刚刚发生的事，`pressure` 说明当前压力，`immediate_objective` 说明角色眼下想解决什么，`deadline` 和 `unresolved_choice` 共同制造行动窗口。`knowledge_gap` 只描述信息差，不替角色补写意志。

`exits` 必须明确暂停、离开或拒绝的可能；环境压力不自动变成许可。`consequence` 只写不处理的剧情后果。

生成局势句：

```text
<角色> 因为 <触发事件>，正在 <地点/关系> 中面对 <当前压力>；
必须在 <期限> 前决定 <未完成选择>，否则 <可见后果> 将发生。
```

## 亲密画像与语态

开局只生成核心子集；进入亲密线后再按需展开。画像描述欲望与表达，许可判定只按 `SKILL.md`。

### 开局核心子集（主 NPC 必填）

```yaml
sexuality_profile:
  baseline: null           # 校验器恢复锚点；概括当前核心画像
  drive:
    intensity: low|medium|high
    awareness: unaware|uncertain|clear
  attraction:
    orientation: unspecified   # 该角色的吸引指向
    toward_player: null        # 对玩家是否具备吸引前提及原因
  preferences:
    initiative: follow|responsive|lead|switch
    pace: gradual|adaptive|direct
    style: tender|natural|playful|intense|experimental|mixed
  expression:
    directness: reserved|natural|direct|uninhibited
  regulation:
    self_control: stable|variable|poor
  interest_origin:
    type: stable|contextual|unexplored|defensive|target_specific
    reason: null   # 例：只对特定对象、源于某段经历、尚未探索
```

重要配角：开局可用同结构核心子集。普通功能配角：仅在剧情需要时生成精简画像。NPC 对玩家的性吸引必须由 `attraction` 的取向与具体原因独立支撑，不因玩家在场自动成立。

### 表里双语态

进入亲密线的角色（主 NPC、重要配角，以及任何实际参与亲密互动的配角）在 9f 语言滤镜里写出两档语态。本节只负责生成两档内容；切换时机与失控写法以 `SKILL.md`「语态调度」「失控语言退化」为准。

- 表层语态：公开身份该有的说话方式。记句长、礼貌度、称呼、回避方式和压力下口吻。身份越端正，此档越不出现性器官粗词。
- 里层语态：卸下身份外壳后的说话方式。记直白程度、惯用荤词、主动或索求的措辞、失控时的口头习惯。
- 切换触发：写明该角色从表层切到里层通常需要什么，例如玩家明确要求、酒意、疼痛、快感累积、关系深化、独处或某个特定称呼。

两档落差由角色决定，不要求人人反差强烈：本就直白的角色两档接近，克制端正的角色落差大。双语态只描述说话方式，不改人格、目标、底线、亲密画像或当前许可。抽取了表层风味与口癖时，两档都应保留可辨认的同一人特征，不写成两个人。

### 按需展开（进入亲密线后）

需要更细写时再补，不要求开局填满：

```yaml
# 可选展开
background: {knowledge, experience, mistaken_beliefs}
preferences_extra: {emotional_link, exclusivity, novelty, turn_ons, turn_offs}
expression_extra: {teasing, verbal_expression, physical_initiative, public_mask, private_style}
regulation_extra: {inhibition, conflict_or_shame, compulsivity, aftermath_need}
```

高欲望或大胆表现不等于 `compulsivity`；只有持续失控并产生负面后果时才记录强迫风险。

### 变化状态

```yaml
sexuality_development:
  baseline: {}          # 开局核心快照
  current: {}           # 当前核心快照
  plasticity: low|medium|high   # 开局可只记总可塑性；展开后再分轴
  evidence: []
  trend: stable|opening|intensifying|conflicted|retreating
```

更新 `current` 须有至少两条跨回合且方向一致的事件证据；变化来自探索或自我承认，不得因压力、边界违反或画像变化自动扩大许可。不得整卡翻转；「沉沦」只作正文主观效果。

## 角色层级与持久化

每个 NPC 保存 `role_level: main|important_supporting|supporting`，用于确定生成和存档深度。多数局只有一名主 NPC；后宫、群像或多主角局允许多名 `main`，每名都须完成完整主 NPC 全维角色卡与合格审计。

- `main`：保存完整核心字段，以及 `identity_profile`、`situation`、`decision_card`、`sexuality_profile`、`sexuality_development` 和 `naming_audit`。
- `important_supporting`：保存核心字段和决策卡；身份、处境与亲密画像按剧情相关性保存。
- `supporting`：保存年龄、身份、功能、当前位置、目标、底线、已知信息、标志细节和自主冷却；其余扩展字段可省略。`core_personality`、`pressure_strategy`、`voice_filter`、`withdrawal_signal`、`emotion` 仅在字段存在时保持字符串；实际进入亲密线前须提升为 `important_supporting` 并补齐这些字段。

角色关系只以顶层 `relationships` 为正式来源。人物卡可在生成阶段计算初始关系，但落档后不得另存会与 `relationships` 冲突的 `relation` 数值。多名 `main` 之间的关系同样只以顶层 `relationships` 为正式来源，须在步骤 14 一并生成。

## 角色决策卡

主 NPC 与重要配角保存决策卡；普通配角用精简值。运行时决策顺序以 `SKILL.md`「NPC 决策」为准。

```yaml
decision_card:
  goal: null               # 校验器恢复锚点；与 goals.primary 保持一致
  stable_core: {identity: null, core_traits: [], hard_limits: []}
  goals: {primary: null, secondary: null, hidden: null}
  knowledge: {known: [], unknown: [], mistaken_beliefs: []}
  pressure_response: {low: null, medium: null, high: null, critical: null}
  action_preferences: {prefer: [], avoid: [], never: []}
  sexuality_refs: {profile: sexuality_profile, development: sexuality_development}
  autonomy: {last_turn: null, cooldown_until: 0}
```

`decision_card.autonomy` 与 NPC 顶层 `autonomy` 使用同一份状态，前者不得形成可独立漂移的副本。正式落档以 NPC 顶层 `autonomy` 的完整结构（含 `recent_turns`）为准；决策卡只引用该状态。

人物决策轴不另存一套副卡：核心价值写入稳定核心或目标，压力策略写入压力反应与行动偏好，关系姿态只落实为性格、语言和行为证据。

## 配角

配角功能独立生成：盟友、阻力、见证者、信息源、竞争者、后果承接者、引路人、误导者、催化剂、庇护者、传话人、监视者或系统自拟。

步骤 13 顺序：功能确认 → 配角简起名 → 核心价值与压力策略 → 其余字段。每名配角保存：姓名、年龄≥18、身份、功能（写入可选扩展字段 `function`）、与玩家/主 NPC 关系（落档于顶层 `relationships`）、目标、底线、已知信息、当前位置、一个标志细节。姓名必须比主 NPC 更普通。重要配角保存亲密核心子集；普通配角仅在剧情相关时生成精简画像。缺合格简审计则步骤 13 未完成。

普通配角实际进入亲密线前，须先即时补齐亲密核心子集、双语态、非空 `core_personality`、`pressure_strategy`、`voice_filter`、`withdrawal_signal`、`emotion`、决策卡与完整自主冷却，再将 `role_level` 提升为 `important_supporting`；保留原稳定 ID、姓名、近期记忆和顶层关系边，不得用升级重建角色或覆盖关系。

## 玩家化身

生成前处理姓名：预锁则只合规与去重；未预锁则标准起名并写入 `naming_audit`。称谓、年龄段、社会位置优先采用结构骰 `玩家称谓` / `玩家年龄段` / `玩家社会位置`；禁止默认写成「先生 + 三十余岁精英男 + 握着对方制度杠杆」。权力结构不推导把柄，也不推导今晚的许可。然后生成：

```text
player_id｜姓名｜年龄≥18｜性别/称谓｜公开身份
外貌与身体基线
可用资源 1-3 项
已知信息起点
社会处境与风险
玩家硬边界列表
```

落档时 `player_naming_audit` 只保留 `chosen` / `source` / `approved_turn` 及必要 checks；候选名单开局用完后不必每回合重读。决策卡是开局校验产物，运行时只读 NPC 顶层的目标、底线、知识和 `autonomy`，不得另写可漂移的副本。

性别/称谓写入可选扩展字段 `appellation`（有值才保存，缺失按姓名与身份自然称呼）；玩家硬边界初始可为空，经「本局不碰 X」命令维护。不得替玩家补写长期目标、内心同意或不可撤回决定。
